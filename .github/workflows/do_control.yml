name: DO_Control_Pipeline_V2

on:
  repository_dispatch:
    types: [do_action]

jobs:
  manage_droplet:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Code
        uses: actions/checkout@v4

      - name: Install doctl
        uses: digitalocean/action-doctl@v2
        with:
          token: ${{ secrets.DO_TOKEN }}

      # --- 关键前置步骤：无论什么任务，先尝试获取现有的 IP ---
      - name: Get Current IP
        run: |
          echo "🔍 检查是否存在名为 minidrop 的服务器..."
          # 抓取 IP 并存入环境变量，供后续所有 SSH 步骤使用
          EXISTING_IP=$(doctl compute droplet list --format "PublicIPv4,Name" --no-header | grep "minidrop" | awk '{print $1}')
          if [ ! -z "$EXISTING_IP" ]; then
            echo "IP=$EXISTING_IP" >> $GITHUB_ENV
            echo "📍 发现现有 IP: $EXISTING_IP"
          else
            echo "❓ 未发现运行中的 minidrop"
          fi

      # --- 功能 1：查询所有服务器并发送 Telegram 消息 (保留) ---
      - name: List All Droplets IP
        if: github.event.client_payload.task == 'list'
        run: |
          echo "🔎 正在抓取账号下所有服务器信息..."
          LIST=$(doctl compute droplet list --format "Name,PublicIPv4,Status" --no-header)
          
          curl -s -X POST https://api.telegram.org/bot${{ secrets.TG_BOT_TOKEN }}/sendMessage \
            -d chat_id=${{ secrets.TG_CHAT_ID }} \
            -d text="📋 当前 DigitalOcean 服务器清单：
            $LIST"

      # --- 功能 2：销毁名为 minidrop 的服务器 (保留) ---
      - name: Destroy Droplet
        if: github.event.client_payload.task == 'delete'
        run: |
          ID=$(doctl compute droplet list --format "ID,Name" --no-header | grep "minidrop" | awk '{print $1}')
          if [ ! -z "$ID" ]; then
            doctl compute droplet delete $ID --force
            
            curl -s -X POST https://api.telegram.org/bot${{ secrets.TG_BOT_TOKEN }}/sendMessage \
              -d chat_id=${{ secrets.TG_CHAT_ID }} \
              -d text="✅ 已成功销毁名为 minidrop 的服务器"
          else
            echo "❓ 未发现名为 minidrop 的服务器"
          fi

      # --- 功能 3：创建最小规格服务器 (512MB) (保留) ---
      - name: Create Droplet
        if: github.event.client_payload.task == 'create'
        run: |
          echo "🏗️ 正在创建 512MB 最小规格服务器..."
          SSH_ID=$(doctl compute ssh-key list --format ID --no-header | head -n 1)
          doctl compute droplet create minidrop \
            --region sfo3 \
            --image ubuntu-24-04-x64 \
            --size s-1vcpu-512mb-10gb \
            --ssh-keys $SSH_ID \
            --wait
          
          # 创建完后更新环境变量里的 IP
          NEW_IP=$(doctl compute droplet list --format "PublicIPv4,Name" --no-header | grep "minidrop" | awk '{print $1}')
          echo "IP=$NEW_IP" >> $GITHUB_ENV
          
          curl -s -X POST https://api.telegram.org/bot${{ secrets.TG_BOT_TOKEN }}/sendMessage \
            -d chat_id=${{ secrets.TG_CHAT_ID }} \
            -d text="🏗️ 服务器创建成功！
            IP 地址：$NEW_IP
            正在配置 s-ui，请稍候 2 分钟..."

      # --- 功能 4：安装 s-ui (独立程序逻辑，支持 create 和 setup) ---
      - name: Install s-ui
        if: github.event.client_payload.task == 'create' || github.event.client_payload.task == 'setup'
        uses: appleboy/ssh-action@v1.0.3
        with:
          host: ${{ env.IP }}
          username: root
          key: ${{ secrets.SSH_PRIVATE_KEY }}
          port: 22
          timeout: 30s
          command_timeout: 15m
          max_uses: 10
          script: |
            echo "🚀 开始安装任务 (类型: ${{ github.event.client_payload.task }})..."
            
            # --- 核心改进：循环等待 SSH 就绪 ---
            echo "正在等待远程 SSH 服务就绪..."
            for i in {1..10}; do
              if systemctl is-active ssh >/dev/null 2>&1; then
                echo "SSH 服务已上线！"
                break
              fi
              echo "等待中 ($i/10)..."
              sleep 10
            done

            # 系统就绪后多给 5 秒缓冲
            sleep 5
            
            # 执行安装 (带自动回车)
            VERSION=1.2.2
            printf "y\n\n\n\n" | bash <(curl -Ls https://raw.githubusercontent.com/alireza0/s-ui/$VERSION/install.sh) $VERSION

      # --- 功能 5：同步数据库 (独立程序逻辑，支持 create 和 setup) ---
      - name: SCP Database to VPS
        if: github.event.client_payload.task == 'create' || github.event.client_payload.task == 'setup'
        uses: appleboy/scp-action@v0.1.7
        with:
          host: ${{ env.IP }}
          username: root
          key: ${{ secrets.SSH_PRIVATE_KEY }}
          source: "s-ui.db"
          target: "/root/"

      # --- 功能 6：后续配置同步与重启 (独立程序逻辑，支持 create 和 setup) ---
      - name: Finalize s-ui
        if: github.event.client_payload.task == 'create' || github.event.client_payload.task == 'setup'
        uses: appleboy/ssh-action@v1.0.3
        with:
          host: ${{ env.IP }}
          username: root
          key: ${{ secrets.SSH_PRIVATE_KEY }}
          timeout: 60s
          max_uses: 5
          script: |
            echo "🔧 正在同步数据库并重启服务..."
            systemctl stop s-ui || true
            
            # 确保目录存在
            [ -d "/usr/local/s-ui/db/" ] || mkdir -p /usr/local/s-ui/db/
            [ -d "/etc/s-ui/" ] || mkdir -p /etc/s-ui/
            
            # 替换数据库
            cp /root/s-ui.db /usr/local/s-ui/db/s-ui.db || true
            cp /root/s-ui.db /etc/s-ui/s-ui.db || true
            chmod 777 /usr/local/s-ui/db/s-ui.db || true
            
            # 重启服务
            systemctl restart s-ui
            echo "🎉 自动换库完成"
            
            curl -s -X POST https://api.telegram.org/bot${{ secrets.TG_BOT_TOKEN }}/sendMessage \
              -d chat_id=${{ secrets.TG_CHAT_ID }} \
              -d text="✅ s-ui 操作已完成！
              任务类型: ${{ github.event.client_payload.task }}
              服务器 IP: ${{ env.IP }}
              数据库已同步并成功重启。"
